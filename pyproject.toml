[project]
name = "forza-horizon-5-telemetry-listener"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0",
    "oracledb>=2.0",
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "websockets>=12.0",
    "httpx>=0.27",
    "pytest>=8.0",
    "pytest-asyncio>=0.23"
]

[dependency-groups]
dev = []

[tool.uv]
